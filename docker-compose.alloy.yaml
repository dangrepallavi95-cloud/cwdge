services:
  site:
    image: nginx:1.27-alpine
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.alloy.conf:/etc/nginx/conf.d/default.conf:ro
