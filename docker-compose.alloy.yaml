services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    network_mode: host
    restart: unless-stopped
    volumes:
      - .:/workspace
    command: php -S 0.0.0.0:3000 -t public public/router.php
